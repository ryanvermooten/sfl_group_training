<p id="notice"><%= notice %></p>

<p>
  <strong>Group name:</strong>
  <%= @group.group_name %>
</p>

<p>
  <strong>Area:</strong>
  <%= @group.area %>
</p>

<p>
  <strong>User:</strong>
  <%= @group.user_id %>
</p>

<%= image_tag @group.group_picture.url(:medium), class: "img_thumbail" %>

<%= link_to 'Edit', edit_group_path(@group) %> |
<%= link_to 'Back', groups_path %>
